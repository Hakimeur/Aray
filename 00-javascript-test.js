"use strict" ;
alert ("Welcome to LA LA LAND !") ;
console.log ('Kawaï\'Gang') ;
let kokoro = "C'est mon coeur", karada = 8n ;
var atama = true, wakaranai=false;
let tableau = [kokoro, karada, atama, wakaranai] ;
const tamashī = "C'est mon âme" ;
console.log (typeof tableau) ;
console.log (tableau[0, 1, 2, 3]) ;

let cible = Math.floor(Math.random()*100);

ask();
function ask(arg = "devine le nombre entre 0 et 100"){
    let valeur = prompt(arg);
}
function check(val){
    if(val < cible){
        ask("C'est plus grand!"");
    }
    else if (val> cible){
        ask("C'est plus petit!");
    }
    else{
        alert("Tu as gagné !")
    }
}
"use strict" ;
alert ("Welcome to LA LA LAND !") ;
console.log ('Kawaï\'Gang') ;
let kokoro = "C'est mon coeur", karada = 8n ;
var atama = true, wakaranai=false;
let tableau = [kokoro, karada, atama, wakaranai] ;
const tamashī = "C'est mon âme" ;
console.log (typeof tableau) ;
console.log (tableau[0, 1, 2, 3]) ;

let cible = Math.floor(Math.random()*100);

ask();
function ask(arg = "devine le nombre entre 0 et 100"){
    let valeur = prompt(arg);
}
function check(val){
    if(val < cible){
        ask("C'est plus grand!"");
    }
    else if (val> cible){
        ask("C'est plus petit!");
    }
    else{
        alert("Tu as gagné !")
    }
}
"use strict" ;
alert ("Welcome to LA LA LAND !") ;
console.log ('Kawaï\'Gang') ;
let kokoro = "C'est mon coeur", karada = 8n ;
var atama = true, wakaranai=false;
let tableau = [kokoro, karada, atama, wakaranai] ;
const tamashī = "C'est mon âme" ;
console.log (typeof tableau) ;
console.log (tableau[0, 1, 2, 3]) ;

let cible = Math.floor(Math.random()*100);

ask();
function ask(arg = "devine le nombre entre 0 et 100"){
    let valeur = prompt(arg);
}
function check(val){
    if(val < cible){
        ask("C'est plus grand!"");
    }
    else if (val> cible){
        ask("C'est plus petit!");
    }
    else{
        alert("Tu as gagné !")
    }
}

